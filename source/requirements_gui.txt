# Python 3.14 lock policy: all runtime dependencies are strictly pinned.
# Regenerate lock candidates with:
#   python -m pip install -U pip pip-tools
#   pip-compile --resolver=backtracking --generate-hashes --output-file source/requirements_gui.lock source/requirements_gui.txt

Pillow==11.0.0
websockets==12.0
bson==0.5.10
Send2Trash==1.8.3
PyOpenGL==3.1.9
# Qt binding: PyQt5 was replaced because Python 3.14 support tracks current PyQt6 wheels.
PyQt6==6.8.1
cryptography==44.0.1
numpy==2.2.2
pygit2==1.17.0
